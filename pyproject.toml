[project]
name = "blog-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Guffran2004-ops", email = "guffranjg.shaik@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pymongo>=4.18.1",
]

[project.scripts]
blog-app = "blog_app:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
